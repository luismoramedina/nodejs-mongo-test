Simple test with node and a mongo user database

config.js needed with this data:

{
	"database_url" : "mongodb://...:...@...:.../..."
}

The database must have this fields at least:
user
access_token
